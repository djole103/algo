# all possible tree structures given num nodes

from tree_utils import *

#def all_trees(num):
#    root = Node(1)
#    num = num - 1
#    nodes = [root]
#    while(num):
#        next_trees = []
#        for t in trees:
#            for leaf in leaf_nodes(t):
#                tree = copy(t)
#                new_trees.append(leaf.left = Node()) 
#                new_trees.append(leaf.right = Node()) 
#        num -= 1


